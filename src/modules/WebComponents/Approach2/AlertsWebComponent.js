import { ReactCustomElement } from 'web-components-with-react';
import { Alert } from 'modules/SharedComponents/Toolbox/Alert';

customElements.define('alerts-web-component', ReactCustomElement(Alert));
